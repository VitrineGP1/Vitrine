<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pagamento Aprovado - Vitrine</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f8f9fa; margin: 0; padding: 20px; }
        .container { max-width: 600px; margin: 50px auto; background: white; padding: 40px; border-radius: 10px; text-align: center; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .success-icon { font-size: 4rem; color: #28a745; margin-bottom: 20px; }
        h1 { color: #28a745; margin-bottom: 20px; }
        p { color: #666; margin-bottom: 30px; line-height: 1.6; }
        .btn { padding: 12px 24px; background: #FF8C00; color: white; text-decoration: none; border-radius: 5px; display: inline-block; margin: 10px; }
        .btn:hover { background: #e67e00; }
    </style>
</head>
<body>
    <div class="container">
        <div class="success-icon">✅</div>
        <h1>Pagamento Aprovado!</h1>
        <p>Seu pagamento foi processado com sucesso. Você receberá um e-mail de confirmação em breve.</p>
        <p>Obrigado por comprar conosco!</p>
        <a href="/" class="btn">Voltar à Loja</a>
        <a href="/meus-pedidos" class="btn">Ver Meus Pedidos</a>
    </div>
    <script>
        // Clear cart and create order after successful payment
        async function createOrderAfterPayment() {
            try {
                const loggedUser = JSON.parse(localStorage.getItem('loggedUser'));
                const cart = JSON.parse(localStorage.getItem('cart')) || [];

                if (loggedUser && cart.length > 0) {
                    const response = await fetch('/api/sales/create-order', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({
                            userId: loggedUser.id,
                            cartItems: cart,
                            paymentId: 'mercado_pago_' + Date.now() // Placeholder para ID do pagamento
                        })
                    });

                    const result = await response.json();

                    if (result.success) {
                        console.log('Pedido criado com sucesso:', result.orderId);
                        // Clear cart only after successful order creation
                        localStorage.removeItem('cart');
                    } else {
                        console.error('Erro ao criar pedido:', result.message);
                    }
                }
            } catch (error) {
                console.error('Erro ao processar pedido após pagamento:', error);
            }
        }

        // Execute order creation
        createOrderAfterPayment();
    </script>
</body>
</html>